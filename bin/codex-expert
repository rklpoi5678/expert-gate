#!/bin/sh
set -eu

if [ "${1:-}" = "exec" ]; then
  shift
  exec codex exec \
    --ignore-user-config \
    --ephemeral \
    --disable plugins \
    --disable hooks \
    --disable apps \
    --disable multi_agent \
    --enable skip_host_skill_discovery \
    "$@"
fi

exec codex "$@"
